import { InputContactFormatDirective } from './input-contact-format.directive';

describe('InputContactFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new InputContactFormatDirective();
    expect(directive).toBeTruthy();
  });
});
